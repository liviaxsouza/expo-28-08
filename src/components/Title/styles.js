import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    title: {
        fontSize: 24,
        marginBottom: 10,
        fontWeight: "bold",
    },
    });

export default styles;